export const API_BASE_URL = 'http://localhost:8000';

export const API_ENDPOINTS = {
  EMAIL_RECORDS: `${API_BASE_URL}/api/email-records`,
  SEND_EMAILS: `${API_BASE_URL}/send-emails`,
  SCHEDULE_EMAILS: `${API_BASE_URL}/schedule-emails`,
}; 